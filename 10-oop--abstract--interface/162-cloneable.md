# Cloneable

- widely used `marker` interface

# shallow copy

- copy object
- copied object is referencing to same data with original object
- no problem if all primitive or immutable

# deep copy

- copy all object including children
- not share reference with original object
